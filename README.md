# heuristic-pathfinding-agent
An optimization search framework that lets virtual agents navigate complex grid mazes efficiently. Implements **A\* Search**, **Dijkstra's Algorithm**, and a **tabular Q-Learning** agent from scratch, benchmarks them across varying obstacle densities, and exports visualization logs of the search process.
